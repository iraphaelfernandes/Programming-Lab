#include <stdio.h>


// Exercício 1.17: Escreva um programa que imprima
// todas as linhas de entrada com mais de 80 caracteres.

#define MAXLINE 1000
#define LIMIT 10

char line[MAXLINE];


// main =====================

int main(void){
  
    
    
  for(i = 0; i < LIMIT -1 && (c = getchar()) != EOF && c != '\n'; ++i){
  
    line[i] = c;
    
  }
    
  
  sizeof();

  return 0;
}

// ==============================
